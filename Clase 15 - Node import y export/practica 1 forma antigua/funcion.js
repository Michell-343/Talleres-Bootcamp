const obtenerTitulo =() =>'Bootcamp fullstack'

obtenerDocente = (docente, mentor) => `El docente del bootcamp es ${docente} y el mentor es ${mentor}` //parametro docente y mentor.

module.exports = {
    obtenerTitulo,
    obtenerDocente
}


/*  const Titulo=() => 'suma'
function suma(a, b){
    return a + b
}
*/